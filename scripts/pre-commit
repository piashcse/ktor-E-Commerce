#!/bin/sh

echo "Running static analysis (ktlint + detekt)..."

./gradlew ktlintCheck detekt

EXIT_CODE=$?

if [ $EXIT_CODE -ne 0 ]; then
    echo "Static analysis failed. Please fix the issues before committing."
    exit $EXIT_CODE
fi

exit 0
